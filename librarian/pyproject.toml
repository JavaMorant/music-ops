[project]
name = "librarian"
version = "0.1.0"
description = "DJ library organiser: a reversible plan/apply/undo engine for the music library"
requires-python = ">=3.12"
dependencies = [
    "typer>=0.12",
    "mutagen>=1.47",
]

[project.optional-dependencies]
# rekordbox XML cue-safety is plain ElementTree, so the core stays light.
# pyrekordbox is now used for read-only rekordbox-DB playlist lookup (dedupe.py);
# reading DB BPM/key is still a later slice. The web review app is an opt-in extra
# so the CLI install stays minimal.
dev = ["pytest>=8.0", "httpx>=0.27"]
web = ["fastapi>=0.111", "uvicorn[standard]>=0.30", "python-multipart>=0.0.9"]
# Ambition-tier `organize`: natural-language → rule-set via Claude. Opt-in so the
# core install stays light; the deterministic `cleanup` is the non-AI equivalent.
ai = ["anthropic>=0.40"]

[project.scripts]
librarian = "librarian.cli:app"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
librarian = ["web/index.html"]
